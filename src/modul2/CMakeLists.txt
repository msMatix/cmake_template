# Add library or executable definitions here For example, if you want to create
# a library from the files in this folder add_library(foo foo.cpp)

# If you need to link this library with others, use target_link_libraries
# Example: target_link_libraries(foo other_libraries)
